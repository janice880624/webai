# draw_string(x, y, txt, color, scale, x_spacing=0, y_spacing=0, mono_space=True)
# draw_string(字元左下角 x 座標, 字元左下角 y 座標, 要顯示的內容, 顏色, 放大倍率, 字符間距, 行間距, 強制文本間距固定)

webai.clear() 
webai.draw_string(100, 100, '哈囉', scale=2)